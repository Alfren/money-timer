# Money timer based on hourly rate

### Start local development server on default port `3000`

`"start": "react-scripts start"`

### Build project

`"predeploy": "npm run build"`

### eploy project to GitHub Pages

`"deploy": "gh-pages -d build"`
